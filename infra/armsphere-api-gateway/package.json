{
  "name": "armsphere-api-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "ArmSphere API Cloudflare Worker — thin proxy via Workers VPC Service",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev":    "wrangler dev --remote"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
