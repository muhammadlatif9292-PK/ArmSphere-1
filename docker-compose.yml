version: '3.9'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:16-alpine
    container_name: armsphere-db
    environment:
      POSTGRES_USER: armsphere
      POSTGRES_PASSWORD: ${DB_PASSWORD:-dev-password-change-in-production}
      POSTGRES_DB: armsphere
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U armsphere"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - armsphere-network

  # API Server
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    container_name: armsphere-api
    environment:
      NODE_ENV: ${NODE_ENV:-production}
      PORT: ${PORT:-4000}
      DATABASE_URL: postgresql://armsphere:${DB_PASSWORD:-dev-password-change-in-production}@postgres:5432/armsphere
      JWT_ACCESS_SECRET: ${JWT_ACCESS_SECRET:?JWT_ACCESS_SECRET must be set}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET:?JWT_REFRESH_SECRET must be set}
      CRON_SECRET: ${CRON_SECRET:?CRON_SECRET must be set}
      STRIPE_WEBHOOK_SECRET: ${STRIPE_WEBHOOK_SECRET:?STRIPE_WEBHOOK_SECRET must be set}
      STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY:?STRIPE_SECRET_KEY must be set}
      AWS_REGION: ${AWS_REGION:-us-east-1}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-}
      LOG_LEVEL: ${LOG_LEVEL:-info}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "4000:4000"
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:4000/health', r => { if (r.statusCode !== 200) process.exit(1); }).on('error', () => process.exit(1))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - armsphere-network
    restart: unless-stopped

  # Admin Web Frontend
  admin-web:
    build:
      context: .
      dockerfile: Dockerfile.admin-web
    container_name: armsphere-admin-web
    ports:
      - "3000:80"
    depends_on:
      - api
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - armsphere-network
    restart: unless-stopped

  # Optional: Adminer for database management (development only)
  adminer:
    image: adminer:latest
    container_name: armsphere-adminer
    environment:
      ADMINER_DEFAULT_SERVER: postgres
    ports:
      - "8080:8080"
    depends_on:
      - postgres
    networks:
      - armsphere-network
    profiles:
      - dev
    restart: unless-stopped

volumes:
  postgres-data:
    driver: local

networks:
  armsphere-network:
    driver: bridge
